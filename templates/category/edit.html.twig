{% extends 'base.html.twig' %}

{% block title %}Edit Category{% endblock %}

{% block body %}
    <div class="container">
        <h3>Edit Category</h3>

            {{ form_start(form) }}
            {{ form_row(form.name, {'label': 'Name'}) }}
            {{ form_end(form) }}
        <a href="{{ path('app_category') }}" class="btn btn-sm btn-outline-secondary">Cancel</a>

    </div>

{% endblock %}

